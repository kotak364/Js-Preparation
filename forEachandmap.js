// 1 Foreach - foreach didn't return new array
//   map -  map return new array

const arr = [10, 20, 30];

// arr.forEach((item, index, arr) => (arr[index] = item + 1));
// const newArr = arr.map((item, index, arr) => item + 1);
// const newArr = arr.forEach((item, index, arr) => item + 1);
// console.log(arr, "arr");
// console.log(newArr, "newArr");


// 2 The map() method is used to transform the elements of an array, whereas the forEach() method is used to loop through the elements of an array.